{
    "name": "Frontend Container",
    "dockerComposeFile": [
        "../../docker-compose.dev.yml"
    ],
    "service": "frontend",
    "shutdownAction": "none",
    "workspaceFolder": "/workspace/frontend",
    // Add your settings here
    "customizations": {
        "vscode": {
            "extensions": [],
            "settings": {}
        }
    },
    "postCreateCommand": "apt-get update -y && npm install && npm start",
    "remoteUser": "root"
}